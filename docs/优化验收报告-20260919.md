# gt-wb-gateway 优化验收报告

日期：2026-09-19
范围：让 Codex（尤其家里 Mac 那台）能真正用本网关跑 agent / function call
验收方式：本机自动化验收，全部证据可复现

---

## 一、结论先行

| 问题 | 答案 |
|---|---|
| Codex 的 function call 到底能不能用 | **能用了。** 真实 `codex exec` 多步 agent 任务端到端通过，工具调用、结果回填、多轮记忆、并行工具调用全部正常 |
| 之前为什么不好用 | **不是 Responses 协议转换问题，也不是"公司电脑配置问题"，是网关自己把请求压坏了** |
| 压坏在哪 | ① 旧投影 >120k 字符就把请求砸成 6k（丢 99%）；② 身份声明句触发上游安全策略 11128；③ 3 个关键字段被静默丢弃；④ 模型名不兜底 |
| 现在成绩 | 回归 **89/89** 通过；真实 agent 任务 **0 次降级、0 次 400**；长上下文保留率 **2.2% → 53.3%** |
| Mac 那台还要改什么 | 接入包补上 `experimental_bearer_token`（已改，实测无 `auth.json` 也能跑通） |

---

## 二、你问的"是协议问题还是配置问题"——都不是

你原话是怀疑"协议 response 问题"或"公司电脑配置问题"。逐条查证后，两个都不是主因：

| 怀疑对象 | 查证结果 | 证据 |
|---|---|---|
| Responses 协议转换有 bug | **协议转换本身是好的**。转换后的 messages、tools、tool_call_id 对应关系全对，模型也确实按工具规范调用 | 三协议冒烟 3/3；真实抓包 `function_call_output` 与 `function_call` 的 `call_id` 一一对应 |
| 公司电脑/网络配置问题 | **不是**。请求能到达上游，只是内容被网关自己的投影层砍了 | 抓包显示上游返回 200 或 400/11128，网络从未超时 |
| Mac 的 Codex 配置问题 | **是次要问题**。`wire_api = "responses"` 本来就配对，但缺 `experimental_bearer_token`，鉴权只押在 `~/.codex/auth.json` 上，一旦缺失就 401 且报错指向"登录"，最难排查 | 见第六节实测 |

**真正的根因在网关内部**——它把一个 47KB 的合法 Codex 请求，处理成了上游要么拦、要么看不懂的东西。

---

## 三、根因诊断（三条，都有实证）

### 根因 1：投影层"二元开关"，一把砍掉 99% 上下文

旧实现：只要请求 >120k 字符，就整包压缩到约 6k。

用真实 Codex 抓包（`_t/cap/req_002.json`，47,053 字符）实测：

| 项目 | 原始 | 旧投影后 |
|---|---|---|
| instructions | 21,026 字符 | **173 字符** |
| 工具 description | 9 个非空 | **全部清空** |
| 仓库指令 / AGENTS.md | 有 | 丢失 |

模型拿不到操作手册，自然不按规范调工具、不收敛——**这才是"function call 不好用"的第一主因**。

### 根因 2：身份声明句触发上游安全策略（code 11128）

无损档首轮仍被上游拒：

```
{"code":11128,"msg":"Illegal API invocation from an unapproved channel"}
```

用 `_t/diag_11128.py` 做逐项减法定位，结论非常干净：

| 减法动作 | 结果 |
|---|---|
| 仅删「身份声明句」（1 处） | ✅ 200 |
| 仅删「Codex 品牌词」（13 处） | ✅ 200 |
| 删掉全部 harness 段 | ✅ 200 |
| **保留身份句、只清空工具描述** | ❌ 400 / 11128 |

→ **唯一触发点就是身份声明句**（"You are a coding agent running in the Codex CLI" 之类），
与指令正文、工具描述、提示词长度都无关。

修法不是"整段压缩"，而是**只中和身份、逐字保留行为指令**（`neutralize_identity_text`）。

### 根因 3：关键字段被静默丢弃

| 字段 | 后果 |
|---|---|
| `reasoning.effort`（嵌套结构） | 推理强度没传上去，agent 决策质量下降 |
| `prompt_cache_key` | 上游缓存失效，每轮都全量重算（也更容易被风控盯上） |
| `parallel_tool_calls` | 模型不敢并行调工具，多步任务轮数翻倍 |
| 模型名不兜底 | 客户端发 `gpt-5.6-luna` / `gpt-6-astra` 这类上游不存在的名字 → 400，**整条 agent 直接崩** |

---

## 四、落地清单（改了什么、在哪、怎么验的）

| # | 优化项 | 改动位置 | 验收证据 |
|---|---|---|---|
| 1 | **弹性上下文预算**：阈值 120k→600k 透明，超出按 60% 弹性保留，不再砸成 6k | `gtwb/responses_projection.py` | 长上下文 A/B：保留率 **2.2% → 53.3%** |
| 2 | **双锚点保留**：原始任务陈述 + 最新用户意图，永久保留不参与压缩 | 同上 | A/B 中"部署口令 + 近期配置"两题全对（旧投影第二题明确答不出） |
| 3 | **无损档脱敏**：零宽空格 + 身份中和，**保留系统提示词全文与工具描述** | `gtwb/desensitize.py` | 无损档首轮 **200**，载荷 47,140 字符（保留 99.8%） |
| 4 | **身份指纹中和**：只改写"我是谁"，不动"做什么" | `gtwb/desensitize.py::neutralize_identity_text` | 无损档 400 → **200**，且 400 条行为指令逐字保留（有回归断言） |
| 5 | **降级档保留真实 system 提示词**（原来直接丢弃） | `gtwb/responses_projection.py` | 回归"压缩档保留真实系统提示词" |
| 6 | **字段透传补齐**：`reasoning.effort`、`prompt_cache_key`、`parallel_tool_calls` | `gtwb/responses_adapter.py`、`gtwb/server.py` | 回归 3 项 + 实测日志出现一轮 2 个 `exec_command` |
| 7 | **工具输出中的图片摘出**：base64 不再被静默丢弃 | `gtwb/responses_adapter.py::_split_tool_output_images` | 回归"图片被摘出为 URL 列表" |
| 8 | **流完整性判定**：200 但流被截断时，发 `response.failed` / `response.incomplete`，不再当成功记账 | `gtwb/responses_adapter.py`、`gtwb/server.py` | 回归 4 项 |
| 9 | **output_index 排序**：先工具后文本时 output 顺序不再乱 | `gtwb/responses_adapter.py::_response_obj` | 回归"output 顺序不乱" |
| 10 | **错误码归正**：NETWORK/SERVER→502、SOFT_RATE→429、AUTH→401、HARD_CREDIT→402、CLIENT→400 | `gtwb/server.py::_status_for_kind` | 回归 4 项 |
| 11 | **模型名兜底/别名** | `gtwb/server.py::resolve_model`、`gtwb/config.py` | 回归 5 项 + 实测未知模型落到兜底并 200 |
| 12 | **审核命中在流开始前紧凑重试一次** | `gtwb/server.py::_execute` | 实测不再需要触发（首轮就过） |

---

## 五、验收结果

### 5.1 回归测试：89 / 89 全过

```
通过 89 项，失败 0 项
```

覆盖：脱敏两档、弹性预算、字段透传、工具输出图片、流完整性、output_index、
错误码归正、模型兜底、记账自洽、诊断抓包。较优化前（53 项）新增 36 项。

### 5.2 三协议工具调用冒烟：3 / 3

对运行中的生产实例（`:8787`）实发带工具请求：

```
✓ RESPONSES 工具调用: function_call name=get_weather args={"city": "Shanghai"}
✓ CHAT      工具调用: tool_call     name=get_weather args={"city": "Shanghai"}
✓ ANTHROPIC 工具调用: tool_use      name=get_weather input={"city": "Shanghai"}
```

三个协议的 tool call 都真的产出、参数都是合法 JSON。

### 5.3 三档对比（同一条真实 Codex 请求，47KB）

| 档位 | 载荷 | 结果 |
|---|---|---|
| none（完全不脱敏） | 47,053 字符 | ❌ 400 / code 11128 |
| **lossless（身份中和）** | **47,140 字符** | ✅ **200，首轮直达** |
| compact（降级） | 6,811 字符 | ✅ 200（现在只是兜底，正常不再触发） |

### 5.4 长上下文 A/B：693,834 字符的真实 Codex 会话

问题：① 部署口令是什么？② retry limit 和 batch size 是多少？

| | 保留率 | 口令 | 配置 |
|---|---|---|---|
| **新投影** | **53.3%**（327,992 字符） | ✓ | ✓ |
| 旧投影 | 2.2%（13,606 字符） | ✓ | ✗ |

旧投影下模型明确回答："我无法回答第二个问题……我不能凭空编造这两个数字。"
——**这就是之前 agent 失忆、乱调工具的直接机制**。

### 5.5 真实 Codex agent 端到端（生产路径 `:8787`）

任务：读取 `mod_a.py`，总结函数个数与名称，写入 `summary.md`。

- 结果：4 轮完成，`finish=stop`，产物 `summary.md` 正确
- 日志统计：`escalated=1` **0 次**、紧凑重试 **0 次**、HTTP 400 **0 次**
- 另有一个 3 轮任务（统计总行数 + 建标记文件）同样 0 降级通过

```
▶ RESPONSES deepseek-v4.1-flash | input_items=3 | projection[transparent] ...
◀ RESPONSES deepseek-v4.1-flash | 200 | 2.4s | finish=tool_calls | tools=['exec_command','exec_command']
```

注意 `tools=[...]` 里一轮出现两个 `exec_command`——**并行工具调用已生效**。

### 5.6 抓包复核：模型确实看得到工具结果

对一次真实 agent 会话逐轮检查：

| 轮次 | input 项 | 工具调用 | 工具结果 |
|---|---|---|---|
| 1 | 3 | 0 | — |
| 2 | 6 | 1 | 339 字符（完整含 exit code） |
| 3 | 9 | 2 | 146 字符 |
| 4 | 12 | 3 | 135 字符 |

**疑似重复调用轮次：0** —— 模型确实读到了上一步结果，没有瞎重试。

> 附注：某次测试模型把 6 行的 `alpha.py` 数成 4 行。追溯抓包发现是**它自己写的 PowerShell
> `Measure-Object -Line` 会忽略空行**，工具返回的原文就是 `alpha.py: 4`，模型如实转述。
> 与网关无关，属于模型选错了统计命令。

---

## 六、Mac 侧配置修正（直接回答"是不是我们这边的配置问题"）

现有 Mac 接入包的 Codex 配置：

```toml
[model_providers.custom]
base_url = "http://192.168.191.81:8787/v1"
wire_api = "responses"          # ✓ 正确
requires_openai_auth = true     # ⚠ 只写这句，key 只能去 ~/.codex/auth.json 取
```

**缺 `experimental_bearer_token`**。风险很隐蔽：一旦 `auth.json` 缺失/过期（换机、
CC Switch 没写、手删过缓存），请求就 401，而 Codex 的报错会指向"登录"，
你很难联想到是接入包的问题。

已修正：

```toml
requires_openai_auth = true
experimental_bearer_token = "<api key>"   # 新增
```

实测验证：**删掉 `auth.json`、只留这个字段，真实 agent 任务照样跑通**（读 42 → 写 84，结果正确）。

同时已刷新 `deploy/home-client-kit-mac/deeplink.txt`（重新导入一次即可生效）。
注意：`deploy/home-client-kit-mac.rar` 是旧打包件，需要时请重新打包。

---

## 七、本次顺带修掉的 3 个自身缺陷

| 缺陷 | 症状 | 处置 |
|---|---|---|
| `RequestStat` 缺 `escalated` 属性 | 非流式请求 500 | 补齐属性 + 加"记账自洽"回归断言 |
| 参数遮蔽同名函数 | `TypeError: 'bool' object is not callable` | 函数改名 `neutralize_identity_text`，保留兼容别名 |
| **诊断抓包长期静默失效** | `capture_dir` 配了却一个文件都不落 | 根因：`server.py` 漏 `import os`，异常被 `except: pass` 吞掉。补 import，并把"首次失败记一条告警"写进代码 |

第三条值得单独说：**旁路功能"开了却没效果"是一种比崩溃更难发现的故障**。
现在同类问题会在日志里留一行 `⚠ 抓包失败（已忽略，不影响请求）`。

---

## 八、已知边界与后续

1. **模型名元数据告警**：日志偶见 `Model metadata for deepseek-v4.1-flash not found`。
   不影响功能（请求正常 200），已在模型兜底里覆盖；如需彻底消除需上游提供模型元数据。
2. **⚠️ 公开仓库推送未完成，且不能盲目推**：
   - 网络层：GitHub 出口当前不通（国内站点 200，走代理访问 github/google 均失败，`schannel: failed to receive handshake`），
     属代理节点问题，非本机故障；
   - 仓库层：本地 OSS 仓库与远端的提交 SHA 不一致——远端最新为 `9ac6167`
     （*docs: add English README*），本地对应提交是 `d024057`（同样信息但 SHA 不同），
     且本地**没有任何远端追踪分支**。这说明本地 `.git` 重建过、历史与远端无共同祖先。
   - 处置：本批改动已在本地提交（`main` 分支，**未推送**）。网络恢复后**不要直接 `git push --force`**，
     先确认远端内容与本地改动前一致（已核对：远端 README 正文与本地改动前完全相同，可判定内容同源），
     再按「fetch → 比对 tree → 决定 rebase 或 force-with-lease」处理。
3. **`home-client-kit-mac.rar` 未重新打包**：`deploy/home-client-kit-mac/` 目录内的
   `deeplink.txt` 已刷新（含 bearer token），分发前请重新打包。
4. **工具轮数会波动**：同一任务实测 3～16 轮不等。已确认无协议缺陷（不是重复调用、不是丢结果），
   属模型自身探索行为。若希望更稳，可在 Codex 侧固定更强模型。

---

## 九、复现命令

```bash
cd /d/workbuddy/研究院/gt-wb-gateway

# 1) 回归
.venv/Scripts/python.exe tests/test_gateway.py

# 2) 三档对比（真实抓包请求）
.venv/Scripts/python.exe _t/diag_400.py _t/cap/req_002.json

# 3) 长上下文 A/B
.venv/Scripts/python.exe _t/accept_longctx.py

# 4) 三协议工具调用冒烟
.venv/Scripts/python.exe _t/smoke_three.py 8787

# 5) 真实 Codex agent（需 Codex CLI）
CODEX_HOME='D:\workbuddy\研究院\gt-wb-gateway\_t\codexhome' \
  codex exec --skip-git-repo-check -C '...\_t\ws4' "读取 mod_a.py，总结函数，写入 summary.md"
```
